import React from "react";
import { Helmet } from "react-helmet";

const Home = () => (
    <main className="container">
        <Helmet>
            <title>SOLPAR</title>
        </Helmet>
    </main>
);

export default Home;
